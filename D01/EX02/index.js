function isBiggerThan(input_num) {
  if (input_num > 4) {
    console.log(`The number ${input_num} is bigger than 4`);
  } else if (input_num === 4) {
    console.log(`The number ${input_num} is equal to 4`);
  } else {
    console.log(`The number ${input_num} is less than 4`);
  }
  //your code
}

// Do not remove or change this line, or the tests won't work
export { isBiggerThan };
